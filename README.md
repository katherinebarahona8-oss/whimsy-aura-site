# Whimsy Horchata — Proportions Fixed

This build fixes the oversized/zoomed hero issue by rendering the hero as a contained image inside a max-width wrapper instead of stretching/cropping it.

Upload all unzipped contents to GitHub:
- `index.html`
- `styles.css`
- `netlify.toml`
- `README.md`
- `assets/`

Then trigger a fresh deploy in Netlify.
